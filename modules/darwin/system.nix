# macOS system defaults — Phase 2.
# Declared settings are re-applied on every rebuild and will override
# changes made by hand in System Settings.
{ ... }:
{
  # Intentionally empty until Phase 2.
}
