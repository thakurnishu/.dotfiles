# Current git branch

`main`

Working directory: /Users/nishantsingh/src/github.com/personal/homelab

Ran `git branch --show-current` (plus `git status -sb` for tracking info).
Branch is `main`, tracking `origin/main`, in sync — no ahead/behind, clean tree.
